@model IEnumerable<Ivoryservices.Models.Category>
 
    @{
    TempData["Admin"] = "ADMIN";
      Layout = "~/Views/Shared/_Layout.cshtml";   
    }


<html lang="en">
<head>
  <title>Bootstrap Example</title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="//code.jquery.com/ui/1.11.4/themes/smoothness/jquery-ui.css">
    <script src="//code.jquery.com/jquery-1.10.2.js"></script>
    <script src="//code.jquery.com/ui/1.11.4/jquery-ui.js"></script>
    @*<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet">*@
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>
</head>
<script>
    $(document).ready(function () {
        alert("hii");
        $("#EnterDate").datepicker();
        dateFormat: "dd-mm-yy",
            changeMonth : true,
                changeYear: true

    });
          });
            });
</script>
<body>

<div>
    <h4 align="center">Category</h4>
    <a asp-action="Category" class="fin">Create New</a>
        @*<label  class="control-label lbl ">Enter Month</label>*@
        <div class="row">
     <div class="col-md-2"></div>
     <div class="col-md-8 card">
        <div class="card-body">
            
<table class="table fin">
    <thead>
        <tr>
            <th>
                CATEGORY NAME
                @*@Html.DisplayNameFor(model => model.Cat_Name)*@
            </th>
            <th>
                CATEGORY IMAGE
               @* @Html.DisplayNameFor(model => model.Cat_Image)*@
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Cat_Name)
            </td>
            <td>
                <img src="~/images/@Html.DisplayFor(modelItem => item.Cat_Image)" width="100px" height="100px" />   <br />
                
            </td>
            @*<td>
                @Html.DisplayFor(modelItem => item.Cat_price)
            </td>*@
            <td>
                <a asp-action="CategoryEdit" asp-route-id="@item.Cat_Id">Edit</a> |
                <a asp-action="CategoryDetails" asp-route-id="@item.Cat_Id">Details</a> |
                <a asp-action="CategoryDelete" asp-route-id="@item.Cat_Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
    </div>
        </div>
    </div>

</div>

</body>
</html>
